<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.qibenyu.explore">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application>

        <activity android:name="com.qibenyu.explore.windowmanager.WindowManagerActivity" />

        <activity android:name=".handler.HandlerActivity" />

        <activity
                android:name="com.qibenyu.explore.glide.GlideActivity"
                android:launchMode="singleTop" />
        <activity android:name=".leak.LeakCanaryActivity" />
        <activity android:name=".broadcast.ReceiverActivity" />
        <activity android:name=".litho.LithoActivity" />
        <activity android:name=".tiktok.TikTokActivity" />
        <activity android:name=".touch.TouchActivity" />

    </application>
</manifest>
